name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: tshirts fail
      run: |
        g++ -Wall -Wextra -Werror tshirts.cpp -o tshirts.out
         .github/workflows/main-workflow.yml ./tshirts.out

    - name: misaligned fail
      run: |
        g++ -Wall -Wextra -Werror misaligned.cpp -o misaligned.out
         .github/workflows/main-workflow.yml ./misaligned.out

    - name: alerter fail
      run: |
        g++ -Wall -Wextra -Werror alerter.cpp -o alerter.out
         .github/workflows/main-workflow.yml ./alerter.out
